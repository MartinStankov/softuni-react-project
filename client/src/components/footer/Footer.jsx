import styles from './Footer.module.css';

export default function Footer() {
    return (
        <footer className={styles.footer}>
            <p>Traveler is a site used to write down about your trips!</p>
        </footer>
    )
}